<div class="container">
    <div class="row justify-content-center">
        <div class="col-12 col-md-6 col-lg-4">
            <h1><%= current_user.admin? ? t(".edit_user") : t(".edit_profile") %> </h1>

            <%= render 'form', user: @user %>

        </div>
    </div>
</div>
