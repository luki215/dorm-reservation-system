<%= form_with(model: aliance, local: true) do |form| %>
  <% if aliance.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(aliance.errors.count, "error") %> prohibited this aliance from being saved:</h2>

      <ul>
      <% aliance.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :founder_id %>
    <%= form.text_field :founder_id %>
  </div>

  <div class="field">
    <%= form.label :note %>
    <%= form.text_area :note %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
