<div class="container">
    <div class="row">
        <div class="col-12">
            <h1><%=t("dashboard.dashboard") %></h1>
            <p><%=t("dashboard.welcome_html")%></p>
            <h2><%=t("dashboard.headers.roundstarts")%></h2>
            <p>
            <table class="table table-striped">
                <thead>
                    <tr>
                        <th><%=t("dashboard.round")%></th>
                        <th><%=t("dashboard.start")%></th>
                    </tr>
                </thead>

                <tbody>

                    <tr>
                        <td><%= t("dashboard.first") %></td>
                        <td><%= AppSetting.first.first_round_start.strftime("%e. %_m. %Y %R") %></td>
                    </tr>
                    <tr>
                        <td><%= t("dashboard.second") %></td>
                        <td><%= AppSetting.first.second_round_start.strftime("%e. %_m. %Y %R") %></td>
                    </tr>
                    <tr>
                        <td><%= t("dashboard.third") %></td>
                        <td><%= AppSetting.first.third_round_start.strftime("%e. %_m. %Y %R") %></td>
                    </tr>
                    <tr>
                        <td><%= t("dashboard.fourth") %></td>
                        <td><%= AppSetting.first.fourth_round_start.strftime("%e. %_m. %Y %R") %></td>
                    </tr>
              </table>
              </p>

<%=t("dashboard.current_round")+": "+t("dashboard."+ AppSetting.first.current_round.to_s) %>

<h2><%= t("dashboard.headers.rounds") %> </h2>

<p><%= t("dashboard.rounds_explanation") %> </p>

<h2><%= t("dashboard.headers.alliance") %> </h2>

<p><%= t("dashboard.alliance_explanation_html") %></p>

<h2> <%= t("dashboard.headers.profile") %> </h2>

<p> <%= t("dashboard.profile") %> </p>

<h2> <%= t("dashboard.headers.dormitory_description") %> </h2>

<p> <%= t("dashboard." + AppSetting.first.dorm.to_s + ".dormitory_description_html") %> </p>

<h2> <%= t("dashboard.headers.faq") %> </h2>

<p><%= t("dashboard.faq_html") %> </p>
        </div>
    </div>
</div>


